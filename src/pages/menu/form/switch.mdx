---
layout: "@layout/main.astro"
title: 开关
---

import Switch from "src/components/switch/index.astro";
import OxalisSwitch from "src/components/switch/oxalis.astro";
export * from 'src/components/markdown/map.mdx';


# switch


<Switch id="switch1" checked />

```pug
span.☘-switch
    input#switch1(type="checkbox" checked)
    label(for="switch1")
```


## `.sm`

<Switch id="switch2" class="sm"/>

```pug
span.☘-switch.sm
    input#switch2(type="checkbox")
    label(for="switch2")
```

## `disabled`

<Switch id="switch3" checked disabled/>
<Switch id="switch4" class="sm" disabled/>

```pug
span.☘-switch
    input#switch3(type="checkbox" checked disabled)
    label(for="switch3")
span.☘-switch.sm
    input#switch4(type="checkbox" disabled)
    label(for="switch4")
```

## custom mover

<Switch id="switch5" dataList={{ primary : "danger" }} class="sm">
    <span>🔥</span>
</Switch>

```pug
span.☘-switch.sm(data-primary="danger")
    input#switch5(type="checkbox")
    label(for="switch5")
        span 🔥
```

## custom style

修改原生css变量 `--switch-size` 和 `--switch-padding` 以覆盖默认尺寸

<OxalisSwitch id="oxalis-switch" />

```pug
span.☘-switch(
    style={
        ["--switch-size"]: "48px",
        ["--switch-padding"]: "4px",
        ["--primary-6"]: "var(--lime-3)",
    }
)
    input#oxalis-switch.oxalis-switch(type="checkbox" checked)
    label(for="oxalis-switch")
        span.oxalis-switch-mover \☘

style(lang="stylus").
    .oxalis-switch>input:checked+label
        .oxalis-switch-mover
            transform rotate(360deg)
            overflow hidden 
```
