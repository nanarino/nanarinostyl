---
layout: "@layout/main.astro"
title: 开关
---

import Switch from "src/components/switch/index.astro";
import OxalisSwitch from "src/components/switch/oxalis.astro";
export * from 'src/components/markdown/map.mdx';


# switch


<Switch id="hello" checked />

```pug
span.☘-switch
    input#hello.☘-input(type="checkbox" checked)
    label(for="hello" title="hello")
```


## `.sm`

<Switch id="small" class="sm"/>

```pug
span.☘-switch.sm
    input#small.☘-input(type="checkbox")
    label(for="small" title="small")
```


## `disabled`

<Switch id="hibit" checked disabled/>
<Switch id="small-hibit" class="sm" disabled/>

```pug
span.☘-switch
    input#hibit.☘-input(type="checkbox" checked disabled)
    label(for="hibit" title="hibit")
span.☘-switch.sm
    input#small-hibit.☘-input(type="checkbox" disabled)
    label(for="small-hibit" title="small-hibit")
```

## custom mover

修改 `☘-switch` 的css变量 `--box-shadow-width-switch` 可以修改阴影效果范围

<Switch id="fire" dataList={{ primary : "danger" }} class="sm" style={{
    "--box-shadow-width-switch": "12px"
}}>
    <span>🔥</span>
</Switch>

```pug
span.☘-switch.sm(data-primary="danger" style={
    "--box-shadow-width-switch": "12px"
})
    input#fire.☘-input(type="checkbox")
    label(for="fire" title="fire")
        span 🔥
```

## custom style

修改全局css变量 `--size-switch` 和 `--padding-switch` 以覆盖默认尺寸

修改 `☘-switch` 的css变量 `--box-shadow-color-switch` 可以单独修改阴影颜色

<OxalisSwitch id="oxalis" />

```pug
span.☘-switch(
    style={
        ["--size-switch"]: "48px",
        ["--padding-switch"]: "4px",
        // 开启的按钮和阴影颜色
        ["--primary-6"]: "var(--lime-6)",
        // 关闭的按钮和阴影颜色
        ["--gray-4"]: "var(--yellow-3)",
    }
)
    input#oxalis.oxalis-switch.☘-input(type="checkbox" checked)
    label(for="oxalis" title="oxalis")
        span.oxalis-switch-mover \☘

style(lang="stylus").
    .oxalis-switch>input:checked+label
        .oxalis-switch-mover
            transform rotate(360deg)
            overflow hidden 
```
