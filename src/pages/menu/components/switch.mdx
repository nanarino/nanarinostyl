---
layout: "@layout/main.astro"
title: 开关
---

import Switch from "src/components/switch/index.astro";
import OxalisSwitch from "src/components/switch/oxalis.astro";
export * from 'src/components/markdown/map.mdx';


# switch


<Switch id="switch1" checked />
<Switch id="switch2" sm/>

```pug
span.☘-switch
    input#switch1(type="checkbox" checked)
    label(for="switch1")
span.☘-switch.sm
    input#switch2(type="checkbox")
    label(for="switch2")
```

## `disabled`

<Switch id="switch3" sm checked disabled/>
<Switch id="switch4" sm disabled/>

```pug
span.☘-switch
    input#switch3(type="checkbox" checked disabled)
    label(for="switch3")
span.☘-switch.sm
    input#switch4(type="checkbox" disabled)
    label(for="switch4")
```

## custom icon

<Switch id="switch5" primary="danger" sm>
    <span>🔥</span>
</Switch>

```pug
span.☘-switch.sm(data-primary="danger")
    input#switch5(type="checkbox")
    label(for="switch5")
        span ❄️
```

## custom style

<OxalisSwitch/>

```pug
span.☘-switch(
    style={
        transform: 'scale(2)',
        margin: '60px',
        '--primary-6': '220 251 124'
    }
)
    input#oxalis-switch(type="checkbox" checked)
    label(for="oxalis-switch")
        span#oxalis-switch-mover \☘

style(lang="stylus").
    #oxalis-switch:checked+label #oxalis-switch-mover
        transform rotate(360deg)
        overflow hidden 
```
