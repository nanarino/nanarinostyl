---
import prefix from "src/scripts/server/prefix";
interface Props {
    id: string
}
const props = Astro.props;
---

<h3 class={`${prefix}-title ${prefix}-font-serif`} id={props.id}>
    <a href={`#${props.id}`} class={`${prefix}-link ${prefix}-connot-select`}>#</a>
    <slot />
</h3>
