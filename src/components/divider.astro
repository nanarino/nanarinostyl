---
import prefix from "src/scripts/server/prefix";
const props = Astro.props;

interface Props {
  tag?: "hr" | "div" | "span";
  style?: Record<string, any> | string;
}
let { tag: Hr } = props;
Hr ||= "hr";
---

<Hr class={`${prefix}-divider`} style={props.style}/>
