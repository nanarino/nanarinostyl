@import './variable/prefix.styl'

.{prefix}-layout
    display flex
    flex-direction column
    flex 1
    &&-has-sider
        flex-direction row
    &-content
        flex 1
    &-sider
        &:first-child
            box-shadow var(--box-shadow-divider-right-lv2)
        &:last-child
            box-shadow var(--box-shadow-divider-left-lv2)
    &-header
        box-shadow var(--box-shadow-divider-bottom-lv2)
    &-footer
        box-shadow var(--box-shadow-divider-top-lv2)
